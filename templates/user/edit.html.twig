{% extends 'base.html.twig' %}

{% block title %}Utilisateur{% endblock %}

{% block body %}
    <h1 class="text-light">Modifier l'utilisateur</h1>

    <div class="card mx-auto p-3" style="max-width: 100%;">
        {{ include('user/_form.html.twig', {'button_label': 'Enregistrer les modifications'}) }}

            <a class="btn btn-outline-dark" href="{{ path('app_user_index') }}">Retour à la liste</a>

            {{ include('user/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
