{% extends "base.html.twig" %}

{% block body %}

    <div class="card text-bg-dark my-3 text-center">
        <img src="{{asset('uploads/img/diamonds.webp')}}" class="card-img rounded-5" alt="images de diamants bleus">
        <div class="card-img-overlay">
            <h3 class="card-title">Une erreur est survenue</h5>
            <p>{{status_code}} - {{status_text}}</p>
        </div>
    </div>

{% endblock %}
