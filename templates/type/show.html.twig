{% extends 'base.html.twig' %}

{% block title %}Type{% endblock %}

{% block body %}
    <h1 class="text-light">Type</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type.name }}</td>
            </tr>
        </tbody>
    </table>

    <div class="justify-content-center d-flex flex-nowrap gap-3">
        <a class="btn btn-outline-dark rounded-3" href="{{ path('app_type_index') }}">Retour à la liste</a>

        <a class="btn btn-outline-dark rounded-3" href="{{ path('app_type_edit', {'id': type.id}) }}">Modifier</a>

        {{ include('type/_delete_form.html.twig') }}
    </div>    
        
{% endblock %}
