{% extends 'base.html.twig' %}

{% block title %}Nouveau type{% endblock %}

{% block body %}
    <h1 class="text-light">Ajouter un type de bijou</h1>

    <div class="card mx-auto p-3" style="max-width: 100%;">
        {{ include('type/_form.html.twig') }}

            <a class="btn btn-outline-dark rounded-3" href="{{ path('app_type_index') }}">Retour à la liste</a>
        </div>
    </div>
    
{% endblock %}
