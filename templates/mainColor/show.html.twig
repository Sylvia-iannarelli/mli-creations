{% extends 'base.html.twig' %}

{% block title %}Couleur principale{% endblock %}

{% block body %}
    <h1 class="text-light">Couleur principale</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mainColor.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ mainColor.name }}</td>
            </tr>
        </tbody>
    </table>

    <div class="justify-content-center d-flex flex-nowrap gap-3">
        <a class="btn btn-outline-dark" href="{{ path('app_mainColor_index') }}">Retour à la liste</a>

        <a class="btn btn-outline-dark" href="{{ path('app_mainColor_edit', {'id': mainColor.id}) }}">Modifier</a>

        {{ include('mainColor/_delete_form.html.twig') }}
    </div>    

{% endblock %}
