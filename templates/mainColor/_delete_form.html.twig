<form method="post" action="{{ path('app_mainColor_delete', {'id': mainColor.id}) }}" onsubmit="return confirm('Confirmer la suppression ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mainColor.id) }}">
    <button class="btn btn-outline-dark me-2"><i class="bi bi-trash"></i></button>
</form>
